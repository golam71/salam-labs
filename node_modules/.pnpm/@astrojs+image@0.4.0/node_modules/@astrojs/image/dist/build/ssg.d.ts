import type { AstroConfig } from 'astro';
import type { SSRImageService, TransformOptions } from '../loaders/index.js';
import { LoggerLevel } from '../utils/logger.js';
export interface SSGBuildParams {
    loader: SSRImageService;
    staticImages: Map<string, Map<string, TransformOptions>>;
    config: AstroConfig;
    outDir: URL;
    logLevel: LoggerLevel;
}
export declare function ssgBuild({ loader, staticImages, config, outDir, logLevel }: SSGBuildParams): Promise<void>;
