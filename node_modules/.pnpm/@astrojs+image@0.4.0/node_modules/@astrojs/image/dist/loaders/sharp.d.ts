/// <reference types="node" />
import type { OutputFormat, SSRImageService, TransformOptions } from './index.js';
declare class SharpService implements SSRImageService {
    getImageAttributes(transform: TransformOptions): Promise<{
        width: number | undefined;
        height: number | undefined;
    }>;
    serializeTransform(transform: TransformOptions): {
        searchParams: URLSearchParams;
    };
    parseTransform(searchParams: URLSearchParams): TransformOptions;
    transform(inputBuffer: Buffer, transform: TransformOptions): Promise<{
        data: Buffer;
        format: OutputFormat;
    }>;
}
declare const service: SharpService;
export default service;
